package main.view.Project;

public enum ImportParams {
    projectId,
    importToken,
    buildName,
    cilink,
    author,
    environment,
    suite,
    testRunId,
    pattern,
    format,
    singleTestRun,
    testNameKey,
    addToLastTestRun
}
